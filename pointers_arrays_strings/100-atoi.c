#include "main.h"

/**
 * _atoi - prints a lotofstuff
 * @s: also probably prints a lot of stuff
 * Return: 0
*/

int _atoi(char *s)
{
	int compteur = 0;

	while (s[compteur] != '\0')
	{
	compteur++;
	}
	return (0);
}
